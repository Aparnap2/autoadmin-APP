# Utility: Navigate to Specific Tab
appId: com.autoadmin.app

# Navigate to specified tab
- tapOn:
    text: "${tab}"
    label: "Navigate to ${tab} tab"

# Wait for tab to load
- waitFor:
    timeout: 5000

# Verify tab content loaded
- assertVisible:
    text: "${tab}"
    regex: true